#include "Bclass.hpp"

/**
 * @brief Default constructor.
 *
 * Initializes the Bclass with a default type.
 */
Bclass::Bclass(void) {}

/**
 * @brief Copy constructor.
 *
 * Creates a new Bclass by copying another one.
 *
 * @param other The Bclass object to copy from.
 */
Bclass::Bclass(const Bclass &other) {}

/**
 * @brief Copy assignment operator.
 *
 * Assigns the values of another Bclass to this instance.
 *
 * @param other The Bclass object to assign from.
 * @return Reference to the assigned Bclass.
 */
Bclass &Bclass::operator=(const Bclass &other) {}

/**
 * @brief Destructor.
 *
 * Called when the Bclass object is destroyed.
 */
Bclass::~Bclass(void) {}
